{
  "rules": {
    "apps": {
      "$appId": {
        "devices": {
          ".read": "auth != null && auth.uid === 'kafrokoRerMRQj4z4i606mFJJKg2'",
          ".indexOn": ["status", "lastSeenAt", "createdAt"],
          "$deviceCode": {
            ".read": "true",
            ".write": "auth != null && auth.uid === 'kafrokoRerMRQj4z4i606mFJJKg2' || !data.exists() && newData.child('status').val() === 'pending' || data.exists() && data.child('status').val() === 'pending' && newData.child('status').val() === 'pending'",
            ".validate": "newData.hasChildren(['status', 'updatedAt']) && $deviceCode.matches(/^[2-9A-HJ-NP-Z]{12}$/) && newData.child('status').val().matches(/^(pending|active|revoked)$/) && (!newData.child('device').exists() || newData.child('device').val().length <= 24) && (!newData.child('school').exists() || newData.child('school').val().length <= 120) && (!newData.child('principal').exists() || newData.child('principal').val().length <= 120) && (!newData.child('phone').exists() || newData.child('phone').val().length <= 40) && (!newData.child('deviceType').exists() || newData.child('deviceType').val().matches(/^(mobile|tablet|desktop|unknown)$/)) && (!newData.child('expiresAt').exists() || newData.child('expiresAt').val() === 0 || (newData.child('expiresAt').isNumber() && newData.child('expiresAt').val() > 1700000000000 && newData.child('expiresAt').val() < 5000000000000)) && (!newData.child('note').exists() || newData.child('note').val().length <= 300) && (!newData.child('ua').exists() || newData.child('ua').val().length <= 200)",
            "school": { ".write": "true", ".validate": "!newData.exists() || newData.isString() && newData.val().length <= 120" },
            "principal": { ".write": "true", ".validate": "!newData.exists() || newData.isString() && newData.val().length <= 120" },
            "phone": { ".write": "true", ".validate": "!newData.exists() || newData.isString() && newData.val().length <= 40" },
            "deviceType": { ".write": "true", ".validate": "!newData.exists() || newData.val().matches(/^(mobile|tablet|desktop|unknown)$/)" },
            "lastSeenAt": { ".write": "true", ".validate": "!newData.exists() || newData.isNumber()" },
            "origin": { ".write": "true", ".validate": "!newData.exists() || newData.isString() && newData.val().length <= 100" }
          }
        }
      }
    }
  }
}
